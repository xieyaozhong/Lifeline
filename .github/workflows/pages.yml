name: Deploy Lifeline to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Validate live task engine
        run: |
          cat app-v2.*.part > /tmp/lifeline-v2.js
          node --check /tmp/lifeline-v2.js

      - name: Prepare static site
        run: |
          mkdir -p _site
          cp index.html style.css features.css app.js app-loader.js app-v2.*.part manifest.webmanifest sw.js icon.svg .nojekyll _site/

      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
